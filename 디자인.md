# 애널리틱스 구축 컨설팅 SaaS — 디자인 가이드

> 컨셉 한 줄: **"딱 봐도 정돈된, 요즘 SaaS 감성"**
> 레퍼런스 무드: Linear(정돈/속도감) · Vercel(여백/대비) · Notion(부드러움) · Stripe(신뢰감)
> 키워드: 깨끗한 여백, 부드러운 라운드, 은은한 그림자, 한 가지 포인트 컬러, 절제된 모션

---

## 0. 디자인 원칙 (5가지)

1. **여백이 곧 고급스러움** — 요소를 빽빽이 채우지 않는다. 카드 사이·섹션 사이 숨 쉴 공간을 둔다.
2. **포인트 컬러는 하나만** — 화면은 대부분 무채색(흰/회/검), 강조는 단 하나의 액센트로.
3. **부드럽게, 그러나 또렷하게** — 라운드 코너 + 얇은 보더 + 낮은 채도 배경, 텍스트는 명확한 대비.
4. **모션은 거들 뿐** — 0.15~0.25s의 미세한 트랜지션. 과한 애니메이션 금지(단, 캐릭터 러너는 예외적 재미 요소).
5. **상태가 한눈에** — 진행/완료/지연을 색과 형태로 즉시 구분.

---

## 1. 컬러 시스템

### 뉴트럴 (기본 골격 — 화면의 90%)

| 토큰 | 라이트 | 용도 |
|---|---|---|
| `bg/base` | `#FFFFFF` | 페이지 배경 |
| `bg/subtle` | `#FAFAFA` | 섹션·사이드바 배경 |
| `bg/card` | `#FFFFFF` | 카드 표면 |
| `border` | `#EAEAEA` | 카드/구분선 |
| `text/primary` | `#18181B` | 제목·본문 |
| `text/secondary` | `#71717A` | 보조 설명 |
| `text/tertiary` | `#A1A1AA` | 플레이스홀더 |

### 액센트 (포인트 — 단 하나)

| 토큰 | 값 | 용도 |
|---|---|---|
| `accent` | `#6366F1` (인디고) | 주요 버튼·링크·진행 바·포커스 |
| `accent/hover` | `#4F46E5` | 호버 |
| `accent/soft` | `#EEF0FF` | 선택 배경·뱃지 |

> 인디고가 식상하면 대안: 바이올렛 `#7C3AED` / 에메랄드 `#10B981` / 코발트 `#2563EB`. **하나만 고른다.**

### 상태 컬러 (의미 전달용, 절제해서 사용)

| 상태 | 컬러 | 사용처 |
|---|---|---|
| 진행중 | `#3B82F6` (블루) | 진행 바, 진행 뱃지 |
| 완료 | `#10B981` (그린) | 완료 체크·취소선 항목 |
| 지연/리스크 | `#F43F5E` (로즈) | 리스크 카드, 기한초과 |
| 보류/대기 | `#F59E0B` (앰버) | 보류 뱃지 |

### 다크 모드 (선택, 있으면 "오 신경썼네")

| 토큰 | 값 |
|---|---|
| `bg/base` | `#0A0A0A` |
| `bg/card` | `#141414` |
| `border` | `#262626` |
| `text/primary` | `#FAFAFA` |
| `text/secondary` | `#A1A1AA` |

---

## 2. 타이포그래피

- **폰트:** `Inter` 또는 `Geist`(영문/숫자), 한글은 `Pretendard` (요즘 한국 SaaS 표준 조합)
- 숫자 강조(진행률 %)는 `tabular-nums` 적용해 자릿수 흔들림 방지

| 역할 | 크기 / 두께 | 비고 |
|---|---|---|
| Display (진행률 %) | 32–40px / 700 | 대시보드 핵심 숫자 |
| H1 페이지 타이틀 | 24px / 600 | |
| H2 섹션 | 18px / 600 | |
| Body | 14px / 400 | 기본 본문 |
| Caption | 12px / 500 | 뱃지·메타정보 |

- 줄간격(line-height): 본문 1.5, 제목 1.2
- 자간(letter-spacing): 큰 제목만 살짝 -0.02em

---

## 3. 간격 · 라운드 · 그림자

### 스페이싱 (4px 기반 스케일)
`4 / 8 / 12 / 16 / 24 / 32 / 48 / 64` — 컴포넌트 내부는 12~16, 카드 사이는 16~24, 섹션 사이는 32~48.

### 라운드 (둥근 정도)
| 토큰 | 값 | 용도 |
|---|---|---|
| `sm` | 8px | 뱃지·인풋 |
| `md` | 12px | 버튼·작은 카드 |
| `lg` | 16px | 메인 카드 |
| `xl` | 24px | 모달·큰 패널 |
| `full` | 9999px | 아바타·칩 |

### 그림자 (아주 은은하게)
```
shadow/sm:  0 1px 2px rgba(0,0,0,0.04)
shadow/md:  0 2px 8px rgba(0,0,0,0.06)
shadow/lg:  0 8px 24px rgba(0,0,0,0.08)
```
> 핵심: 그림자는 **연하게**. 진한 그림자는 촌스러움의 지름길. 보더 + 옅은 그림자 조합이 요즘 감성.

---

## 4. 레이아웃

```
┌──────────────────────────────────────────────┐
│  좌측 사이드바 (240px)  │   메인 콘텐츠 영역      │
│  - 로고                 │   - 상단 헤더(브레드크럼)│
│  - 프로젝트 스위처       │   - 콘텐츠(카드 그리드)  │
│  - 메뉴(대시보드/WBS/   │                        │
│    주간보고/회의록/      │                        │
│    택소노미/대시보드)    │                        │
└──────────────────────────────────────────────┘
```

- **사이드바:** `bg/subtle` 배경, 현재 메뉴는 `accent/soft` 배경 + `accent` 텍스트
- **콘텐츠 최대 폭:** 1200~1280px, 가운데 정렬, 좌우 여백 넉넉히
- **그리드:** 대시보드 위젯은 12컬럼 기반, 카드 간 gap 16~24px
- **반응형:** 데스크톱 우선(컨설팅 도구라 PC 사용 중심), 태블릿까지 대응

---

## 5. 핵심 컴포넌트 스타일

### 버튼
| 종류 | 스타일 |
|---|---|
| Primary | `accent` 배경 + 흰 글자, 라운드 `md`, hover 시 `accent/hover` |
| Secondary | 흰 배경 + `border` + `text/primary` |
| Ghost | 배경 없음, hover 시 `bg/subtle` |
- 높이 36~40px, 패딩 좌우 16, 폰트 14/500
- 트랜지션 `all 0.15s ease`

### 카드 (대시보드 위젯)
- `bg/card` + `border` 1px + `shadow/sm` + 라운드 `lg`
- 내부 패딩 20~24px, 상단에 작은 라벨(`text/secondary`, 12px) + 큰 값

### 뱃지 / 상태 칩
- 라운드 `full`, 패딩 좌우 10 / 상하 4, 12px/500
- 상태 컬러의 `soft` 배경 + 진한 텍스트 (예: 완료 = 그린 배경 톤 + 그린 텍스트)

### 진행률 표시
- 얇은 바(높이 6~8px, 라운드 full), 트랙은 `border` 색, 채움은 `accent`
- 옆에 % 숫자(`tabular-nums`)
- **캐릭터 러너:** 진행 바 위에 작은 캐릭터(Lottie)가 진행률 위치에 서서 달리는 모션 → 제품의 시그니처 재미 포인트

### 체크리스트 / 완료 표기
- 미완료: 빈 체크박스 + `text/primary`
- 완료: `accent`/그린 체크 + 텍스트 **취소선 + `text/tertiary`** 회색 처리

### 간트차트(WBS)
- 작업 막대: 라운드 `sm`, 단계별로 톤 다르게(같은 액센트 계열의 명도 변화 권장 — 무지개색 금지)
- 의존관계 선: `text/tertiary` 얇은 선
- 오늘 날짜: `accent` 세로 점선

---

## 6. 아이콘 · 일러스트

- **아이콘:** `Lucide` (얇고 일관된 선, 요즘 표준) — 16~20px, `text/secondary`
- **일러스트/캐릭터:** Lottie 기반 귀여운 러너 캐릭터(진행률), 빈 상태(Empty state)에도 작은 일러스트로 친근함
- **빈 상태(Empty State):** "아직 회의록이 없어요" 같은 안내 + 작은 일러스트 + CTA 버튼 → 휑하지 않게

---

## 7. 모션 가이드

| 상황 | 모션 |
|---|---|
| 버튼/링크 hover | 0.15s 색·배경 전환 |
| 카드 진입 | fade + 8px 슬라이드 업, 0.2s |
| 패널/모달 | 0.2s ease-out 슬라이드 |
| 진행률 변화 | 0.4s ease 바 채움 + 캐릭터 이동 |
| 체크 완료 | 살짝 통통 튀는 스프링(미세하게) |

> 원칙: 빠르고 가볍게. 사용자를 기다리게 하는 모션은 금지.

---

## 8. "완성도 높아 보이는" 디테일 체크리스트

이런 작은 것들이 "오 여기까지 했구나" 느낌을 만듭니다.

- [ ] 포커스 링(`accent` 2px)으로 키보드 접근성 표시
- [ ] 로딩은 스피너 대신 **스켈레톤 UI**
- [ ] 숫자는 `tabular-nums`로 흔들림 없게
- [ ] 빈 상태마다 일러스트 + 안내 문구
- [ ] 토스트 알림(우측 하단, 부드럽게 등장/퇴장)
- [ ] hover 시 커서·배경 피드백 일관성
- [ ] 다크 모드 지원(있으면 가산점)
- [ ] 일관된 라운드/여백/보더 (제멋대로 X)
- [ ] 컬러는 무채색 + 액센트 1개 규칙 끝까지 지키기

---

## 9. 토큰 요약 (개발 적용용)

```css
:root {
  /* neutral */
  --bg-base: #FFFFFF;
  --bg-subtle: #FAFAFA;
  --bg-card: #FFFFFF;
  --border: #EAEAEA;
  --text-primary: #18181B;
  --text-secondary: #71717A;
  --text-tertiary: #A1A1AA;

  /* accent */
  --accent: #6366F1;
  --accent-hover: #4F46E5;
  --accent-soft: #EEF0FF;

  /* status */
  --status-progress: #3B82F6;
  --status-done: #10B981;
  --status-risk: #F43F5E;
  --status-hold: #F59E0B;

  /* radius */
  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 16px;
  --radius-xl: 24px;

  /* shadow */
  --shadow-sm: 0 1px 2px rgba(0,0,0,0.04);
  --shadow-md: 0 2px 8px rgba(0,0,0,0.06);
  --shadow-lg: 0 8px 24px rgba(0,0,0,0.08);
}
```

> Tailwind + shadcn/ui 조합이면 위 토큰을 `tailwind.config`의 theme에 그대로 매핑하면 됩니다.

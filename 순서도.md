# 자동태깅 — 쉬운 설명서

> **한 줄 요약:** 웹사이트 주소를 넣으면, 로봇이 버튼을 찾고 AI가 이름을 붙여 주고, 사람이 고른 뒤 **태깅 설명서(Excel)** 를 만들어 주는 프로그램입니다.

---

## ★ 전체 큰 그림 (한 장에 다 보기)

아래 그림 **하나**만 보면, 로그인부터 Excel 저장까지 **전부** 이어집니다.  
(가운데 **Phase 1 / Phase 2**만 동시에 진행되는 부분입니다.)

```mermaid
flowchart TB
    START((시작))

    LOGIN["🔐 Microsoft 로그인\n누가 썼는지 확인 · 14일 유지"]
    S0["📁 0단계 · 프로젝트 선택\n새로 만들거나 고르기\n→ 이 프로젝트에만 데이터 저장"]
    S1["📝 1단계 · 사이트 입력\n웹 주소 · PC 또는 MO\n탭 이름(비우면 AI가 자동으로 이름)"]
    S2["▶ 2단계 · 분석 시작\n여러 URL이면 줄 세워서 실행\n끝난 job은 아래로 · 다시 분석은 버튼만"]

    KEY["🔑 Firecrawl 키 풀\n5개 키 중 잔량 많은 것 자동 선택\n분석 끝날 때까지 같은 키 유지"]
    OPEN["🤖 로봇 브라우저로 사이트 열기\n(Firecrawl — 다른 컴퓨터의 가짜 크rome)"]
    CLEAN["🧹 화면 정리\n팝업·쿠폰창 닫기 · 숨은 메뉴 펼치기"]
    FIND["🔍 버튼·링크·메뉴 찾기\n각각에 번호 tag_id 붙이기\nPC용 / MO용 구분"]
    POS["📍 위치 저장\nx,y,w,h 좌표 → positions.json"]
    PAGE["📸 전체 화면 사진 1장\npage_view · pc.png 또는 mo.png"]
    AI["🧠 AI 이름 붙이기\n버튼 이름 · 묶음 · 페이지 이름\n(OpenRouter / Gemini)"]
    P1{{"✅ Phase 1 끝\n후보 목록 완성"}}

    S3["👆 3단계 · 후보 선택\n왼쪽: 후보 목록 · 오른쪽: 사진 미리보기\n클릭하면 그 위치로 스크롤"]
    WAIT["⏳ 아직 사진 없으면\n「이미지 캡쳐중…」 표시"]
    TOOLS["🔎 도구\n전체 위치 보기 · AI로 위치 맞는지 검사"]
    PICK["✔ 필요한 것만 체크"]

    P2JOB["📷 Phase 2 · 백그라운드\n버튼마다 사진 찍기\ntags/1.png, tags/2.png …"]
    P2DONE["사진 하나씩 DB·화면에 반영"]

    S4["📋 4단계 · 택소노미 초안\n공통(GNB·Footer) / PC / MO 탭\n표에서 수정 → 후보 JSON도 같이 변경"]
    S5["📤 5단계 · 검토 & Excel\n최종 확인 후 Excel 파일 받기"]

    DB[("💾 데이터베이스\n프로젝트 · 후보 · 택소노미 · 체크 상태\n사진 주소(URL)")]
    DISK[("📁 서버 폴더 data/captures/\n실제 PNG 사진 · 좌표 파일")]
    END((끝))

    START --> LOGIN --> S0 --> S1 --> S2
    S2 --> KEY --> OPEN --> CLEAN --> FIND --> POS --> PAGE --> AI --> P1

    P1 --> S3
    P1 -.동시에 뒤에서.-> P2JOB
    P2JOB --> P2DONE
    P2DONE -.사진 준비될 때마다.-> S3

    S3 --> WAIT
    WAIT -.1.8초마다 확인.-> S3
    S3 --> TOOLS --> PICK
    PICK --> S4 --> S5

    S5 --> DB
    S5 --> DISK
    DB --> END
    DISK --> END

    style P1 fill:#FDE68A,stroke:#CA8A04
    style P2JOB fill:#FDE68A,stroke:#CA8A04
    style S3 fill:#FCE7F3,stroke:#DB2777
    style AI fill:#DBEAFE,stroke:#2563EB
    style DB fill:#D1FAE5,stroke:#059669
    style DISK fill:#E0E7FF,stroke:#4F46E5
```

### 그림 읽는 법 (3가지만 기억)

1. **위에서 아래로** 쭉 내려가면 됩니다. (로그인 → 0~5단계 → 저장 → 끝)
2. **Phase 1**이 끝나면 **3단계 화면이 바로** 열립니다. 사진(Phase 2)은 **옆길**에서 뒤늦게 채워집니다.
3. **글**은 DB에, **사진 파일**은 폴더에 저장됩니다. 둘 다 있어야 미리보기가 잘 보입니다.

---

## 1. 이 프로그램이 하는 일

웹사이트에 있는 **버튼·메뉴·링크**를 찾아서,

1. **번호**를 붙이고  
2. **이름**을 지어 주고 (AI)  
3. **사진**을 찍고  
4. 사람이 **필요한 것만 고르고**  
5. **Excel 설명서**로 내보냅니다.

분석 결과는 **프로젝트**마다 따로 저장됩니다. A 프로젝트와 B 프로젝트 데이터는 섞이지 않습니다.

---

## 2. 쓰는 기술 (쉬운 말로)

| 이름 | 뭐 하는 거예요? | 비유 |
|---|---|---|
| **웹 화면** (HTML · CSS · JavaScript) | 버튼, 목록, 미리보기를 보여 줌 | TV 화면 |
| **서버** (Node.js · Express) | 분석 명령을 받고 일을 시킴 | 교실 선생님 |
| **데이터베이스** (PostgreSQL / PGlite) | 프로젝트, 후보, 택소노미 **글자 정보** 저장 | 공책 |
| **Microsoft 로그인** (Azure AD) | 누가 로그인했는지 확인 (14일 유지) | 출석부 |
| **Firecrawl** | 다른 컴퓨터의 **로봇 브라우저**로 사이트를 열어 줌 | 원격 로봇 |
| **AI** (OpenRouter · Gemini) | 버튼 이름·페이지 이름을 지어 줌 | 똑똑한 도우미 |
| **캡처 사진** (PNG 파일) | 화면·버튼 사진 | 찍은 사진 앨범 |
| **Excel** (xlsx) | 택소노미 설명서 파일 | 최종 숙제 |

> **Firecrawl 키:** `FIRECRAWL_API_KEYS`에 여러 개 등록 → 서버가 **잔량 많은 키**를 골라 씀 → 화면 위 **키 합계** 표시

---

## 3. 화면 6단계 (마법사)

| 단계 | 이름 | 하는 일 |
|:---:|---|---|
| **0** | 프로젝트 선택 | 프로젝트 만들거나 고르기. 이후 모든 작업은 이 프로젝트에만 저장 |
| **1** | 사이트 입력 | URL, PC/MO, 탭 이름(비우면 AI가 자동으로 이름 지음) |
| **2** | 분석 실행 | 로봇 + AI가 사이트 분석. 완료 job은 목록 아래, **다시 분석**은 버튼만 |
| **3** | 후보 선택 | 왼쪽 목록 · 오른쪽 미리보기 · 스크롤 · 전체 위치 · AI 검증 |
| **4** | 택소노미 초안 | 공통 / PC / MO 탭. 표 수정 → 후보·JSON 동기 |
| **5** | 검토 & 보내기 | Excel 다운로드 |

---

## 4. Phase 1 · Phase 2 (왜 나누나?)

| | Phase 1 (먼저) | Phase 2 (뒤에서) |
|---|---|---|
| **하는 일** | 찾기 · 번호 · AI 이름 · 후보 목록 | 버튼마다 사진 찍기 |
| **사람** | **바로** 3단계 화면 사용 | 기다리면 사진이 채워짐 |
| **안 되면** | 분석 실패 메시지 | 「캡쳐중…」 또는 「다시 분석」 |

→ 위 **전체 큰 그림**에서 `Phase 1 끝` → `3단계`로 바로 가고, `Phase 2`는 점선(옆길)로 이어집니다.

---

## 5. 저장되는 것 (어디에?)

| 저장소 | 들어 있는 것 |
|---|---|
| **DB** | 프로젝트, URL, 후보, 택소노미, 체크 여부, **사진 주소(URL)** |
| **폴더** `data/captures/{job_id}/` | **pc.png / mo.png**, **tags/*.png**, **positions.json** |

브라우저 **localStorage는 쓰지 않습니다.** 프로젝트를 다시 열면 **DB에서** 불러옵니다.

---

## 6. PC / MO / 공통

- 같은 URL도 **PC용**과 **MO(휴대폰)용**을 **따로** 분석할 수 있습니다.
- **GNB**(위 메뉴) · **Footer**(아래 메뉴)는 **공통** 탭에 모읍니다.
- Excel도 **공통 / PC / MO** 시트로 나뉩니다.

---

## 7. 서버에 올릴 때

- **Node.js** 서버 + **DB** + **captures 폴더** 세트로 옮깁니다.
- `.env`: Microsoft, `FIRECRAWL_API_KEYS`, AI 키
- 여러 사람이 쓰면 **키 풀** + **프로젝트별 DB**로 섞이지 않습니다.

---

**기억할 것 세 가지**

1. **프로젝트** 단위로 데이터가 나뉜다.  
2. **Phase 1 끝나면 바로 고를 수** 있고, 사진은 뒤에서 채워진다.  
3. **글은 DB, 사진은 폴더** — 둘 다 백업해야 한다.

---

*최종 정리: 2026-07-15 · 개발용 상세는 `PLAN.md` 참고*
